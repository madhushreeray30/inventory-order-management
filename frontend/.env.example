# Base URL of the backend API. For local dev (vite) this points at the
# backend running on your machine. In production set it to the deployed
# backend URL (e.g. https://your-api.onrender.com).
VITE_API_URL=http://localhost:8000
