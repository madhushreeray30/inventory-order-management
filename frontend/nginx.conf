server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: send all unknown routes to index.html so
    # client-side routing (react-router) works on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
