# Backend configuration — copy to .env and fill in real values.
# Connection string for SQLAlchemy + psycopg (v3).
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/inventory

# Comma-separated allowed CORS origins (your frontend URL(s)).
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
